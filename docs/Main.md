# Main.java - Entry Point

## 📋 ภาพรวม
ไฟล์หลักที่เป็นจุดเริ่มต้นของเกม รับผิดชอบการสร้างหน้าต่างเกมและแสดงหน้าเมนู

## 🔧 Methods

### `public static void main(String[] args)`
**หน้าที่**: จุดเริ่มต้นของโปรแกรม
**การทำงาน**:
1. ตั้งค่า `sun.java2d.uiScale = "1.0"` เพื่อปิด DPI scaling ป้องกันภาพยืด
2. รัน `createAndShowGameWindow()` บน Event Dispatch Thread
3. เป็น entry point ที่ JVM เรียกเมื่อเริ่มโปรแกรม

### `private static void createAndShowGameWindow()`
**หน้าที่**: สร้างและแสดงหน้าต่างเกม
**การทำงาน**:
1. สร้าง JFrame ใหม่ด้วยชื่อ "Tower Defense"
2. เรียก `configureWindow()` เพื่อตั้งค่าหน้าต่าง
3. สร้าง MenuPanel และเพิ่มเข้าไปใน JFrame
4. เรียก `finalizeWindow()` เพื่อแสดงหน้าต่าง

### `private static void configureWindow(JFrame window)`
**หน้าที่**: ตั้งค่าพื้นฐานของหน้าต่าง
**การทำงาน**:
1. `setDefaultCloseOperation(EXIT_ON_CLOSE)` - ปิดโปรแกรมเมื่อปิดหน้าต่าง
2. `setResizable(false)` - ไม่ให้ปรับขนาดหน้าต่างได้

### `private static void finalizeWindow(JFrame window)`
**หน้าที่**: จัดเตรียมและแสดงหน้าต่าง
**การทำงาน**:
1. `pack()` - ปรับขนาดหน้าต่างให้พอดีกับ components
2. `setLocationRelativeTo(null)` - วางหน้าต่างตรงกลางจอ
3. `setVisible(true)` - แสดงหน้าต่าง

## 🎯 Design Pattern
- **Factory Pattern**: แยกการสร้าง window ออกเป็น methods ย่อย
- **Single Responsibility**: แต่ละ method มีหน้าที่เดียว